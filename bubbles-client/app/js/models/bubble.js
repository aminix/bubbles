'use strict';

/*
angular.module('model')
	.factory('Bubble', function() {
		function bubble(previous_bubble, next_bubble, info) {
			var model = {};
			
			model.previous_bubble = previous_bubble;
			model.next_bubble = next_bubble;
			model.info  = info || {
				title : "My info",
				description : "this is the description",
				impact : 30
			};
			model.cx = 40;
			model.cy = 40;
			
			model.translate = function(x, y) {
				model.cx = x;
				model.cy = y;
			}
			
			return model;
		}
		return bubble;
	})*/

